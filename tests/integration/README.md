# Integration tests

Cross-service tests will live here when a feature defines the required disposable dependencies and synthetic fixtures. Bootstrap service health is currently tested within each application.
