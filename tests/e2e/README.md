# End-to-end tests

Repository-wide journeys belong here once product workflows exist. The web bootstrap smoke test is colocated under `apps/web/tests/e2e` and uses only public, non-sensitive content.
