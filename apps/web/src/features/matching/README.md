# Matching and discovery

Search, item detail, match list, and comparison interfaces live in `src/pages/DiscoveryPages.tsx`.

They intentionally show integration-pending or empty states until public-safe report and ranking contracts exist. Match scores assist discovery only and never establish ownership.
